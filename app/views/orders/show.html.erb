<h1>訂單編號 <%= @order.id %></h1>

<p>訂單狀態: <%= @order.status %></p>
<p>Amount: <%= @order.amount %></p>

<% @order.order_items.each do |item| %>
  <li><%= item.product.name %> 數量<%= item.qty %></li>
<% end %>
<hr>
<% if @order.status == "new" %>
  付款按鈕
<% end %>
